<script>
    const langEl= document.querySelector('.langWrap');
    const link= document.querySelectorAll('a');
    const titleEl= document.querySelector('.title');
    const descEl= document.querySelector('.description');

    link.forEach(eL=>{
        else.addEventListener('click,()=>{
            langEl.querySelector('.active').clasList.remove('active');
            el.classList.add('active');
            const attr=el.getAttribute ('language');
            titleEL.textContent=data[attr].title;
            descrEl.textContent=data[attr].description;
      });
    });
var data={
    "english":
    "grid-item item-1":"Server",
    "grid-item item-2":"Recomendation tips",
    "grid-item item-3":"Our best Recomendations"
    "descripcio":"The servers we recommend are low budget, so they are not able to perform high load computing work, the vast majority carry windows server operating systems and prices are around € 500
    "
}
</script>